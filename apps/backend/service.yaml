# Owner: 정태훈
# Scope: Application Runtime / GitOps
# Related: seokpan-gitops#29

apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: application
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: backend
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
